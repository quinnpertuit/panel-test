import React from 'react';
import { View, Text } from 'react-native';

const MainLayout = () => {
    return (
        <View>
            <Text>Electric Heating Table</Text>
        </View>
    );
};

export default MainLayout;
